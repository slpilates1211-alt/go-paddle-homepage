# go-paddle-homepage — 프로젝트 가이드

KSUPA(대한패들보드연맹)의 SUP 강사·지도자 과정 홈페이지 저장소입니다.

## 브랜드 컬러 (KSUPA 로고 기준) — 모든 디자인에 반드시 적용

KSUPA 로고/배너는 **스카이 블루(시안) 워드마크 + 레드 패들러**의 2색 구성입니다(플래그 배리언트: 화이트·레드·스카이블루·옐로우·블랙). 이 저장소에서 만드는 **모든 HTML/페이지/공지/배너 등 시각 요소는 아래 팔레트를 사용**하세요. 값은 로고 컬러 이미지에서 도출했습니다. 이전에 쓰던 청록(teal) 계열은 사용하지 않습니다.

| 토큰 | HEX | 역할 |
| --- | --- | --- |
| Sky Blue (브랜드 본색) | `#29ABE2` | 로고 시안. 밝은 강조·그라데이션 밝은 쪽·라디얼 하이라이트 (흰 텍스트 배경으로는 대비 부족 → 텍스트 얹지 말 것) |
| Blue (구조 기본) | `#0E86C4` | 흰 텍스트가 얹히는 곳: 헤더·번호배지·링크·시간표·체크박스 |
| Blue Dark | `#0A6294` | 그라데이션 어두운 쪽·링크 hover |
| Navy (base) | `#082F4A` | 최심층 배경·푸터·본문 제목 |
| Brand Red (accent) | `#ED1C24` | **포인트 전용** — 주요 CTA, 가격/핵심 숫자, eyebrow 바, '마감임박' 상태, 레벨2 |
| Red Dark | `#C8151C` | 레드 hover/눌림 |
| Light Blue | `#9BD7F2` | 어두운 배경 위 강조 텍스트 |
| Blue Tint | `#E8F6FC` | 옅은 시안 배경(정보 박스 등) |
| Off-white | `#F5FAFD` | 페이지 배경 |
| Yellow (선택) | `#FFE400` | 로고 배너의 옐로우 배리언트. 블루·레드만으로 부족해 **더 다양한 컬러가 필요할 때** 3차 포인트 색으로 사용 |
| Ink / Muted / Line | `#12222E` / `#5A6B76` / `#E2EBF1` | 본문·보조·구분선 |

### 사용 원칙
- **블루가 주(主), 레드는 포인트(點).** 로고처럼 화면 대부분은 블루로 구성하고, 레드는 시선을 모아야 하는 소수 요소에만 씁니다. 레드를 넓게 쓰면 경고/오류처럼 보이니 피합니다.
- **가격·핵심 숫자는 레드**로 강조합니다(포스터의 "25만원"과 동일한 규칙).
- **리스트 체크박스·체크표시·시간표 등 반복 요소는 블루**로 둡니다(레드로 하면 오류처럼 보임).
- **과정 레벨 색 코드**: 레벨1 = 블루, 레벨2 = 레드, 레벨3 = 네이비, 인트로 = 그레이.
- 로고 마크 박스는 블루→레드 그라데이션(`linear-gradient(135deg,#0e77cc,#e62733)`)으로 표현하면 로고 느낌을 살릴 수 있습니다.
- 폰트는 한글 가독성을 위해 **Pretendard**(웹) / 시스템 산세리프 폴백을 사용합니다.

> 참고: **SSUMSS 네이버 카페 배너**(`assets/naver-cafe-banner/`)는 KSUPA가 아닌 별개 브랜드이므로 이 팔레트 적용 대상이 아닙니다.

## 주요 산출물

- `instructor-course.html` — SUP 강사과정 소개 랜딩 페이지
- `notices/` — 회차별 교육공지 HTML (`*-board.html` = 게시판 붙여넣기용 인라인 버전)
- `.claude/skills/sup-course-announcement/` — 교육공지 자동 생성 스킬 (텍스트 + HTML). 템플릿에 위 팔레트가 이미 반영되어 있습니다.
